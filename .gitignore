# Virtual environment
venv310/
venv/
env/
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Dataset and pose data (too large for git)
dataset/
pose_data/

# Large model files (use Git LFS or upload separately)
models/*.h5
models/*.keras
models/fold_*.keras

# NumPy data files (regenerated by pipeline)
features.npy
labels.npy
confusion.npy

# Keep these small metadata files
# classes.npy  ← KEEP (tracked by git)
# metrics.txt  ← KEEP
# metrics_detailed.txt ← KEEP

# Databases (contain user data — never commit)
*.db
history.db
users.db

# MediaPipe model (large binary)
pose_landmarker.task

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/settings.json
.idea/
*.swp

# Logs
*.log

# Streamlit cache
.streamlit/secrets.toml

# Jupyter
.ipynb_checkpoints/
*.ipynb